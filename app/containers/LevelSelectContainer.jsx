import { connect } from 'react-redux';
import React, {Component} from 'react'; 

const mapStateToProps = (state) => {
  return {}
}

const LevelSelectContainer = (props)=>{
  return (
    <div>   </div>
  )
}


export default connect(mapStateToProps)(LevelSelectContainer);
